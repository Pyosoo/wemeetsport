// ** React Imports
import { useState, SyntheticEvent, Fragment, ReactNode } from 'react';
import useMediaQuery from '@mui/material/useMediaQuery';

const NotificationDropdown = () => {
  return <>Noti Drop down</>;
};

export default NotificationDropdown;
