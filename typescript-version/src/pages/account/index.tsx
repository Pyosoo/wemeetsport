import { useEffect, useState } from 'react';
import Box from '@mui/material/Box';

const Page = () => {
  useEffect(() => {
    // 알림 리스트 받아오기 API
  }, []);

  return <div>ss</div>;
};

export default Page;
